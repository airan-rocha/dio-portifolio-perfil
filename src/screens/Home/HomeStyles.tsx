import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#5b637b',
  },
  imagePerfil: {
    width: 300,
    height: 300,
    borderRadius: 300,
    borderColor: '#000',
    borderWidth: 1,
  },
  scrollContainer: {
    width: '100%',
  }
});